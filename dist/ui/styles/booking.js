/**
 * Booking detail, reschedule and cancel dialogs. EMPTY on purpose: wave 2
 * fills it. Read only `var(--cw-account-*)`, prefix every rule with
 * `.carisma-portal` (or `.cw-dialog` for a <dialog> in the top layer), and
 * reuse the primitives in base.ts (.cw-btn, .cw-chip, .cw-card, .cw-facts).
 */
export const PORTAL_BOOKING_CSS = ``;
//# sourceMappingURL=booking.js.map